class Solution {
    public int singleNumber(int[] nums) {
        int t = 0, i =0 ;
           Arrays.sort(nums);
            while(i < nums.length-2 && nums[i]==nums[i+1]){
                i = i+2;
            }
            t = nums[i];
        return t;
    }
}
